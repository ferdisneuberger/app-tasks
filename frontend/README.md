# Frontend

Frontend React minimalista para o `backend-v3`.

## Rodando

```bash
copy .env.example .env
npm install
npm run dev
```

## Ambiente

- `VITE_API_URL`: URL base da API. Exemplo: `http://localhost:3000/api`
